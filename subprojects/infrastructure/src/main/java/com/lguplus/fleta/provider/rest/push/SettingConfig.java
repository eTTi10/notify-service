package com.lguplus.fleta.provider.rest.push;

public class SettingConfig
{
    /*
    push.server.ip = 211.115.75.227 # 서버 IP
    push.server.port = 9600			# Port
    push.socket.max = 20			# 최대 유지 소켓 연결 수
    push.socket.min = 3				# 최소 유지 소켓 연결 수
    push.socket.close_secend = 90	# 연결된 소켓의 유효 시간 (1분 30초)
    push.socket.timeout = 2000		# 소켓 Connection Timeout 2초
    push.socket.channelNum = 0000	#
    push.socket.initCnt = 3			# 기동 시점에 강제로 만드는 소켓 수
    push.socket.channelID = PsAgt	# 채널 식별자 : psAgt{0000~9999} / 소켓 생성시 부여
    push.cp.destination_ip = 222.231.13.85	# 푸시 Destination IP
    push.call.retryCnt = 2			# 전송 오류시 재 전송 횟수
    */
}
